// ThemeSelector — selezione tema visivo (minimal, neon, carbon…)
export default function ThemeSelector({ current, onSelect }) {
  return <div className="theme-selector">{/* theme options */}</div>;
}
